export class LogoutUser {
    async execute(): Promise<{ success: boolean; message: string }> {
      // despues veo como hago esta logica, o si pueden haganla, porfavor
      return { success: true, message: 'Logout successful' };
    }
  }
  